{
    "root_agent": [],
    "expert_agents": [
        {
            "name": "ImageGenerationAgent",
            "enable": true,
            "description": "Generate one or more images from text only. This agent does not accept reference images. It supports provider selection, where `nano_banana` is the default and `seedream` can be requested explicitly. For Gemini image generation you can also pass optional `aspect_ratio` and `resolution` values.",
            "parameters": "`{'prompt': ['prompt1', 'prompt2', ...], 'provider': 'nano_banana|seedream'(optional), 'aspect_ratio': '16:9'(optional), 'resolution': '1K|2K|4K'(optional)}`"
        },
        {
            "name": "ImageEditingAgent",
            "enable": true,
            "description": "Edit one or more existing images. `input_name` can include the image to edit plus optional reference images. If multiple input images are provided, the prompt should explain their order. This agent supports provider selection, where `nano_banana` is the default and `seedream` can be requested explicitly.",
            "parameters": "`{'input_name': ['image name1', ...], 'prompt': ['prompt1', 'prompt2', ...], 'provider': 'nano_banana|seedream'(optional)}`"
        },
        {
            "name": "KnowledgeAgent",
            "enable": true,
            "description": "Perform art design and text optimization. Given the original task and optional reference images, this agent generates a professional design plan and image generation prompts derived from those inputs. It is best suited for tasks that need specialized visual design, promotional marketing effects, or detailed analysis of reference images for precise image generation. Ordinary image generation does not require this agent. This agent can complete more than one design and prompt at the same time, but the task must clearly specify how many designs and prompts are required.",
            "parameters": "`{'prompt': 'original task', 'input_name': ['image name1', ...](optional)}`"
        },
        {
            "name": "ImageUnderstandingAgent",
            "enable": true,
            "description": "Analyze the image and output text, has three modes: description, style, ocr, the respective functions are: Analyzing image content; Analyze the artistic style of images; Analyze the text in the image",
            "parameters": "`{'input_name': 'name', 'mode': 'description|style|ocr'}`"
        },
        {
            "name": "ImageToPromptAgent",
            "enable": true,
            "description": "Reverse engineer one or more input images into detailed image-generation prompts. Use this when the goal is to infer a likely creation prompt for later reuse, variation, or recreation, rather than just describing the image contents.",
            "parameters": "`{'input_name': 'name'}` or `{'input_name': ['name1', 'name2']}`"
        },
        {
            "name": "SearchAgent",
            "enable": true,
            "description": "Search the Internet for pictures or text related to the input parameter query, has three modes: image, text, all. How to choose mode: If the task requires images as a reference, choose `image`; If the task requires obtaining knowledge-based information such as design concepts, choose `text`; If both, choose `all`",
            "parameters": "`{'query': 'content need to be search', 'mode': 'image|text|all'}`"
        }
    ]
}
